import express from 'express';

export default {
    editModuleInfo: async (req: express.Request, res: express.Response) => {},
    editModuleCategoryInfo: async (req: express.Request, res: express.Response) => {},
};
