import express from 'express';

export default {
    addDevice : (req: express.Request, res : express.Response) => {

    },

    addCategory : (req: express.Request, res : express.Response) => {

    },

    addEnvironment : (req: express.Request, res : express.Response) => {

    },
}